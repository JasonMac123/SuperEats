import { View, Text } from "react-native";
import React from "react";

const ParallaxScrollView = () => {
  return (
    <View>
      <Text>ParallaxScrollView</Text>
    </View>
  );
};

export default ParallaxScrollView;
